{
  "name": "tap",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf tests/*test*.ts",
    "build": "node createTests.js",
    "test": "yarn clean && yarn build && node --experimental-specifier-resolution=node --loader='ts-node/esm' --loader='esmock' --test tests/*"
  },
  "peerDependencies": {
    "axios": "^1.2.5"
  },
  "dependencies": {
    "esmock": "^2.1.0"
  }
}
