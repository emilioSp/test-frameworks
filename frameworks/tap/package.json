{
  "name": "tap",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --trace_gc --experimental-specifier-resolution=node --loader='ts-node/esm' --loader='esmock' --test assignDriver.test.ts",
    "clean-test-memory": "rm -rf ./memory-consumption/memory*",
    "build-test-memory": "node createMemoryTests.js",
    "test-memory": "yarn clean-test-memory && yarn build-test-memory && node --trace_gc --experimental-specifier-resolution=node --loader='ts-node/esm' --loader='esmock' --test ./memory-consumption/*"
  },
  "peerDependencies": {
    "axios": "^1.2.5"
  },
  "dependencies": {
    "esmock": "^2.1.0"
  }
}
