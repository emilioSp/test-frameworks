{
  "name": "vitest",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf tests/*test*.ts",
    "build": "node createTests.js",
    "test": "yarn clean && yarn build && vitest run --threads=false"
  },
  "peerDependencies": {
    "axios": "^1.2.5"
  },
  "dependencies": {
    "vitest": "^0.28.1"
  }
}
